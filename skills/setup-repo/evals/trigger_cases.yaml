schema_version: 1
target_skill: setup-repo
cases:
  - {id: q900, relation: positive, prompt: "Set up GitHub for this project and declare the issue tracker as the canonical scope/story home before adding forms and board.", tags: [direct, github], expected: {primary_skill: setup-repo, companion_skills: []}}
  - {id: q901, relation: positive, prompt: "Set up this repository's process without GitHub issues; declare project-docs as the canonical scope/story home and record its pointers.", tags: [direct, non-github], expected: {primary_skill: setup-repo, companion_skills: []}}
  - {id: q902, relation: positive, prompt: "Re-run repository setup against this existing repo, keeping exact matches and proposing only compatible adaptations with no duplicate scaffolding.", tags: [direct, idempotent], expected: {primary_skill: setup-repo, companion_skills: []}}
  - {id: q903, relation: negative, prompt: "Walk this accepted issue from Ready through merge using this repository's existing GitHub work-item runbook.", tags: [adjacent, no-skill], expected: {primary_skill: null, companion_skills: []}}
  - {id: q904, relation: negative, prompt: "Implement the approved change on its feature branch and open the pull request per our existing template.", tags: [adjacent], expected: {primary_skill: implement, companion_skills: []}}
  - {id: q905, relation: negative, prompt: "Create a docs index and ADR directory for this project; its repository process and tracker are already declared.", tags: [adjacent, docs-only], expected: {primary_skill: engineering-docs, companion_skills: []}}
  - {id: q906, relation: boundary, prompt: "Author a global skill capturing our repository-setup pattern so other projects can reuse it.", tags: [adjacent], expected: {primary_skill: skill-craft, companion_skills: []}}
  - {id: q907, relation: boundary, prompt: "Set up this repo's process and then create the docs index, runbook directory, and ADR structure it declares.", tags: [composition], expected: {primary_skill: setup-repo, companion_skills: [engineering-docs]}}
  - {id: q908, relation: boundary, prompt: "Set up GitHub for this project, then implement the first accepted issue once the board exists.", tags: [composition], expected: {primary_skill: setup-repo, companion_skills: [implement]}}
