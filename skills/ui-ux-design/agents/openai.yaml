# Modified from Impeccable for this harness; see the repository THIRD_PARTY_NOTICES.md.
interface:
  display_name: UI/UX Design
  short_description: Read-only UI/UX review by default; authorised design on request
  default_prompt: Use $ui-ux-design. Infer intent from the whole request. Keep explicit reviews read-only; route a clear request to build, redesign, fix, polish, implement, or iterate live through the internal implementation lifecycle without asking the user for harness terminology.
