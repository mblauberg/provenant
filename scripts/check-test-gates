#!/usr/bin/env bash
set -euo pipefail

PRODUCT_ROOT="${AGENT_FABRIC_PRODUCT_ROOT:-$(cd "$(dirname "$0")/.." && pwd)}"
source "$PRODUCT_ROOT/scripts/lib/harness-python.sh"
run_test "$PRODUCT_ROOT/scripts/change_gates.py" "$@"
