#!/bin/sh
set -eu

script_dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
product_root=$(dirname -- "$script_dir")

npm ci --prefix "$product_root" --no-audit --no-fund
node "$product_root/runtime/agent-fabric/scripts/write-npm-ci-attestation.mjs" "$product_root"
